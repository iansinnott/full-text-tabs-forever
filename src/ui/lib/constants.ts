/** the minimum char length of a query */
export const MIN_QUERY_LENGTH = 3;

export const routeLabels: Record<string, string> = {
  index: "Search",
  "database-repl": "SQL",
  settings: "Settings",
};
